snippet prelude
  // ; exec go run "\$0" "\$@"
  package main

  import (
    "fmt"
    "log"
    "os"
    "path/filepath"
  )

  func main() {
    self, err := os.Executable()
    if err != nil {
      log.Fatal(err)
    }
    dir := filepath.Dir(self)
    os.Chdir(dir)
    fmt.Println(dir)

    $0
  }


snippet bug
  fmt.Printf("%#v\\n", $0)
