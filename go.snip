snippet prelude
  // ; exec go run "\$0" "\$@"
  package main

  import (
    "fmt"
    "os"
    "path/filepath"
  )

  func main() {
    self, err := os.Executable()
    if err != nil {
      panic(err)
    }
    dir := filepath.Dir(self)
    os.Chdir(dir)
    fmt.Println(dir)

    $0
  }

