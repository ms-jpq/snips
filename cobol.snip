snippet prelude
  #!/usr/bin/env -S -- bash -Eeuo pipefail -O dotglob -O nullglob -O extglob -O failglob -O globstar -x
         *> . || cobc -Wall -x "\$0" -o "\${TMP:="\$(mktemp)"}" && "\$TMP" "\$@"; exec -- rm -v -fr -- "\$TMP"
         >>SOURCE FORMAT FREE

  PROGRAM-ID. Hello.
  PROCEDURE DIVISION.

  DISPLAY "Hello, world!".
  STOP RUN.

  $0
